# Development Dockerfile for Node.js with nodemon hot reload
FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install

# Copy source code
COPY . .

ENV NODE_ENV=development
ENV PORT=3000
EXPOSE 3000

# Use nodemon for development with hot reload
CMD ["npm", "run", "dev"]
